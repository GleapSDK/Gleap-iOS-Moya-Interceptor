import XCTest
@testable import Gleap_iOS_Moya_Interceptor

final class Gleap_iOS_Moya_InterceptorTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Gleap_iOS_Moya_Interceptor().text, "Hello, World!")
    }
}
